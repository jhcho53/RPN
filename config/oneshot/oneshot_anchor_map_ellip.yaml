model:
  use_residual: true
  use_sparse: true
  anchor_learnable: true
  anchor_mode: map
  anchor_alpha: 0.7
  geometry: elliptic

loss:
  mu_scaleinv: 0.1
  w_lidar: 0.3
  w_anchor_reg: 0.05

train:
  tag: mcprop_1shot_ellip_anchor_map
  epochs: 200
  lr: 0.001
  save_dir: runs_oneshot
